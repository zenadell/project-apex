Objective: Build me a fully playable Flappy Bird clone using HTML5 Canvas and JavaScript. Ensure it includes canvas auto-scaling, bird with wing animation, pipe gradients, AABB collision, gravity 0.4, flap -6, requestAnimationFrame loop, game states (ready/playing/game-over), difficulty increase (gap -5% per 5 points, speed +10% per 5 points), score display, input handling (click/touch/spacebar with prevention of double-jump), and restart functionality. Everything must be in a single index.html file.
Context: 
Architecture constraints: {"stack":["HTML5","CSS3","JavaScript"],"structure":{"index.html":"Single self-contained file with canvas element, embedded CSS for responsive scaling, and inline JavaScript implementing the full game: Flappy Bird clone with wing animation (frame swapping), pipe gradients, AABB collision, gravity (0.4) and flap (-6), requestAnimationFrame loop, game states (ready, playing, game-over), dynamic difficulty (gap -5% and speed +10% every 5 points), score display, input handling (click, touch, spacebar with double-tap guard), and restart mechanism."},"dependencies":[],"devDependencies":[],"entrypoint":"index.html","buildCommand":"npx serve .","testCommand":"echo \"No automated tests; open index.html in a browser to test gameplay.\"","designPatterns":["Game Loop Pattern","State Pattern","Module Pattern (IIFE)"],"notes":"All graphics are procedurally drawn; no external assets. Canvas auto-scales while maintaining aspect ratio. Difficulty increments are applied immediately on score milestones and reset on restart. Input listeners are bound once and filtered by state to avoid double-taps."}

CRITICAL LESSONS LEARNED FROM PAST FAILURES:
- Fix applied for Warden Rejection: The 'main' field is set to 'src/index.js', but this file does not exist in the Golden Blueprint structure. The blueprint specifies the entry point as 'src/script.js'. This misalignment could cause runtime errors if anyone runs 'node .' or if the project is imported as a module.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The package.json file does not include the 'setup' script as specified in the Golden Blueprint, which is responsible for creating necessary folders using fs.mkdirSync.; The package.json file does not include the .eslintrc.js and jest.config.js configurations as specified in the Golden Blueprint.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: requirements.txt includes a development dependency (pytest) and is missing production dependencies (requests, python-dotenv) as specified in the Golden Blueprint. The Golden Blueprint requires 'requests' and 'python-dotenv' in requirements.txt, and pytest belongs in dev-requirements.txt.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The attempted content includes opencv-python, Pillow, and pytest, which are not specified in the Golden Blueprint's production dependencies. The Golden Blueprint requires only requests>=2.31.0 and python-dotenv>=1.0.0 in requirements.txt. Additionally, pytest is a dev dependency and belongs in dev-requirements.txt, not requirements.txt. This violates the Single Responsibility and Dependency Injection design patterns by introducing unnecessary and unapproved dependencies.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The attempted content adds 'opencv-python' and 'Pillow' which are not part of the blueprint's production dependencies, and includes 'pytest' which should only be in dev-requirements.txt. The correct production dependencies per the Golden Blueprint are 'requests>=2.31.0' and 'python-dotenv>=1.0.0'.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The requirements.txt includes opencv-python and Pillow, which are not part of the Golden Blueprint. The blueprint specifies only 'requests' and 'python-dotenv' as dependencies. Adding extraneous libraries deviates from the intended architecture of a lightweight HTTP-based camera capture module.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The Golden Blueprint specifies config.py should hold DEFAULT_SOURCE = 'default_test.pdf', timeout settings, and other constants for a pipeline project. The attempted code introduces a full pydantic-settings based configuration for an APEX TTS application with unrelated settings (database, cache, server, TTS voices, etc.), which is completely outside the blueprint's domain. This overwrites the intended simple configuration with a complex, unrelated module, violating the architecture and likely breaking dependent modules that expect simple constants.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: Incorrect exception handling for network errors. The code catches built-in 'TimeoutError' and 'ConnectionError', but the 'NetworkFileDataSource' uses the requests library which raises 'requests.exceptions.Timeout' and 'requests.exceptions.ConnectionError' instead. This means network timeouts and connection errors will not trigger the fallback logic, violating the blueprint's requirement to handle network timeouts via requests.; Fallback logging message 'No source provided – using sample.' is printed multiple times (when source is None/empty, and again in each fallback exception branch). The Golden Blueprint note states 'Logging ensures the required message is printed exactly once when fallback activates.' It should only be printed when the fallback is actually triggered due to missing/unusable source, and not redundantly.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The file contains two conflicting code blocks: a shim entrypoint that imports from a non-existent 'run' module and a second block that defines main() with argparse. This results in invalid Python (duplicate if __name__ blocks, redefinition of main). The shim references a 'run' module not specified in the Golden Blueprint, which is a hallucination.; The Golden Blueprint specifies the optional CLI argument as '--source', but the code uses '--data-source'. This breaks consistency with the blueprint and likely with the Pipeline class expectation.; The code calls logging.basicConfig in main(), but the Golden Blueprint delegates logging configuration to pipeline/logger.py. Setting it here may override or duplicate the intended logging setup.. Ensure strict adherence to Warden rules and valid syntax.
- Fix applied for Warden Rejection: The require paths use './config', './generateMock', './fileWriter', './startServer' but according to the Golden Blueprint structure these modules are located under 'src/' (e.g., src/config.js). This will cause runtime errors because the files are not at the expected locations.. Ensure strict adherence to Warden rules and valid syntax.
Do NOT repeat these mistakes!

You are the ApexForge Engine (powered by Hermes). Please generate the files outlined in the architecture structure EXACTLY in the current working directory (./). DO NOT create a wrapper project folder (e.g., 'flappy-bird' or similar). Use your tools to write to disk. DO NOT stop until the files are written.