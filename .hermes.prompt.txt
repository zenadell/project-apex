Objective: Capture photo using native macOS tools (capture_a_photo_using_native_macos_tools) with fallback to Unified Camera Capture and then PIL placeholder; save to dashboard/selfie.jpg; perform permission check.
Context: 
Architecture constraints: {"stack":["python3","opencv-python","Pillow","imagesnap (macOS native capture utility)"],"structure":{"capture_photo.py":"Main entry point: orchestrates photo capture via fallback chain (imagesnap → OpenCV → PIL placeholder), performs camera permission check, ensures dashboard/ directory, saves selfie.jpg.","test_capture.py":"Unit tests for capture functions using mocked subprocess and OpenCV; validates fallback logic and file output.","requirements.txt":"Python dependencies: opencv-python, Pillow."},"dependencies":["opencv-python","Pillow"],"devDependencies":["pytest","pytest-mock"],"entrypoint":"capture_photo.py","buildCommand":"pip install -r requirements.txt && python capture_photo.py","testCommand":"python -m pytest test_capture.py -v","designPatterns":["Strategy Pattern (each capture method encapsulated as a strategy)","Chain of Responsibility (fallback attempts in order until success)","Facade Pattern (permission check and directory creation abstracted)"],"notes":"Native capture relies on imagesnap being installed via `brew install imagesnap`. OpenCV fallback accesses camera 0 and requires macOS camera permission (prompt handled by OS). Permission check performs a quick OpenCV probe (attempts to read one frame) to detect authorization status. PIL placeholder generates a solid 640x480 image. Output saved to dashboard/selfie.jpg."}

You are the ApexForge Engine (powered by Hermes). Please generate the files outlined in the architecture structure EXACTLY in the current working directory (./). DO NOT create a wrapper project folder (e.g., 'flappy-bird' or similar). Use your tools to write to disk. DO NOT stop until the files are written.